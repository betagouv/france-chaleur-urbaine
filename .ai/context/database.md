# Data Model

## Schema overview

PostgreSQL 16 with PostGIS 3.5. 99+ tables. Full TypeScript types generated by `kysely-codegen` in `src/server/db/kysely/database.ts`.

Core entities:
```
User ←→ Gestionnaire assignment ←→ Network (reseaux_de_chaleur)
User → Demand (connection request)
Demand → Network (proximity match)
Network → Zone (zone_de_developpement_prioritaire)
Job (async processing queue)
Event (audit log)
```

### Key tables

| Table | Purpose |
|-------|---------|
| `users` | User accounts (email, password hash, role) |
| `reseaux_de_chaleur` | District heating networks (geometry, capacity, energy mix) |
| `reseaux_de_froid` | District cooling networks |
| `zones_et_reseaux_en_construction` | Planned networks and development zones |
| `demands` | Connection requests from citizens/professionals |
| `demand_emails` | Email history for demand communications |
| `pro_eligibility_tests` | Bulk address test batches |
| `pro_eligibility_tests_addresses` | Individual address test results |
| `bdnb_batiments` | National building database entries |
| `besoins_en_chaleur_batiments` | Building-level heat demand |
| `batiments_raccordes_reseaux_chaleur_froid` | Buildings connected to networks |
| `jobs` | Async job queue (status, type, payload) |
| `events` | Audit/event log |
| `api_accounts` | API key management |

### Spatial tables (PostGIS)

| Table | Geometry | Purpose |
|-------|----------|---------|
| `reseaux_de_chaleur` | MultiLineString | Network pipe routes |
| `zones_et_reseaux_en_construction` | MultiPolygon | Planned zones |
| `zone_de_developpement_prioritaire` | MultiPolygon | Priority development areas |
| `ign_communes` | MultiPolygon | Municipality boundaries |
| `zone_a_potentiel_chaud` | MultiPolygon | Heat potential zones |
| Various `*_tiles` tables | Mixed | Pre-computed map tiles |

## ORM: Kysely (NOT Prisma)

Kysely is a type-safe SQL query builder. Types are generated from the actual DB schema.

- Client singleton: `src/server/db/kysely/` (pool: 5-60 connections, timeout: 30s).
- Application name: `FCU-API`.
- Never call Kysely directly from components or tRPC routes — always go through services.

### Common patterns

```ts
// Select with conditions
const networks = await db
  .selectFrom('reseaux_de_chaleur')
  .select(['id', 'nom_reseau', 'Identifiant reseau'])
  .where('has_trace', '=', true)
  .$if(!!region, (qb) => qb.where('region', '=', region))
  .orderBy('nom_reseau', 'asc')
  .execute();

// Insert
await db
  .insertInto('demands')
  .values({ email, address, status: 'pending', created_at: new Date() })
  .returning('id')
  .executeTakeFirstOrThrow();

// Update
await db
  .updateTable('demands')
  .set({ status: 'contacted', updated_at: new Date() })
  .where('id', '=', demandId)
  .execute();

// Transaction
await db.transaction().execute(async (tx) => {
  const demand = await tx.insertInto('demands').values({...}).returning('id').executeTakeFirstOrThrow();
  await tx.insertInto('events').values({ type: 'demand_created', entity_id: demand.id }).execute();
});
```

### PostGIS patterns

**Coordinate systems:**
- Storage: Lambert 93 (EPSG:2154) — French national projection.
- Input/Output: WGS84 (EPSG:4326) — GPS coordinates.
- Always transform between systems in queries.

```ts
// Distance calculation
sql`ST_Distance(
  ST_Transform(geom, 2154),
  ST_Transform(ST_SetSRID(ST_MakePoint(${lon}, ${lat}), 4326), 2154)
)`

// Point-in-polygon test
sql`ST_Contains(geom, ST_Transform(ST_SetSRID(ST_MakePoint(${lon}, ${lat}), 4326), 2154))`
```

### Query best practices

- Always use `.select([...])` — never rely on `selectAll()` in production queries (over-fetches columns).
- Prevent N+1: use joins or batch queries, never loop with individual DB calls.
- Use `.$if()` for optional filters rather than building query strings conditionally.

### Conditional queries

Use `.$if()` for optional filters (avoids SQL injection risks from string concatenation):
```ts
query
  .$if(!!status, (qb) => qb.where('status', '=', status))
  .$if(!!region, (qb) => qb.where('region', '=', region))
```

## Naming conventions (database)

- Tables: snake_case, often French names (historical): `reseaux_de_chaleur`, `besoins_en_chaleur_batiments`.
- Columns: mixed (snake_case and French): `nom_reseau`, `Identifiant reseau`, `created_at`.
- Foreign keys: `<table>_id` pattern.
- Geometry columns: `geom` (PostGIS).
- Enums: stored as text in most cases.

> Note: Column naming is inconsistent due to legacy data imports. Do not rename existing columns — it would break data pipelines.

## Migration workflow

Location: `src/server/db/migrations/`
Format: `YYYYMMDDHHMMSS_description.ts`

```ts
import { type Kysely, sql } from 'kysely';

export async function up(db: Kysely<any>): Promise<void> {
  await sql`ALTER TABLE demands ADD COLUMN priority TEXT DEFAULT 'normal'`.execute(db);
}

export async function down(db: Kysely<any>): Promise<void> {
  await sql`ALTER TABLE demands DROP COLUMN priority`.execute(db);
}
```

**Rules:**
1. Never edit migrations after they've been applied to any environment.
2. Run `pnpm db:sync` after migration to update Kysely types.
3. For type updates after migration: prefer `pnpm db:sync --single` (manual merge) over full regeneration.
4. Commit migration file + updated `database.ts` types together.
5. Use `sql` template literals for raw SQL in migrations.
6. Always provide both `up` and `down` functions.

## Common index patterns (in migrations)

```sql
-- JSONB index
CREATE INDEX idx_table_field ON table USING GIN (field jsonb_path_ops);

-- PostGIS spatial index
CREATE INDEX idx_table_geom ON table USING GIST (geom);

-- Partial index
CREATE INDEX idx_demands_pending ON demands (created_at) WHERE status = 'pending';
```

## MCP Postgres (interactive queries)

When running SQL queries via MCP postgres for the user:
- Always display the SQL query at the end of the response in a blockquote: `> **SQL**` followed by a code block.
- This applies to all queries: analysis, debugging, data exploration.

## Seeding / bootstrap

- Bootstrap from production: `pnpm db:bootstrap` (via Scalingo CLI).
- Test users: `pnpm cli users:add <email> <password> <role>`.
- No traditional seed file — data comes from production snapshots.
