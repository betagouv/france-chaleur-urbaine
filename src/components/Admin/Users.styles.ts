import styled from 'styled-components';

export const TableContainer = styled.div`
  padding: 64px;
  display: flex;
  flex-direction: column;
  align-items: center;
  .fr-input-group {
    position: absolute;
    left: 50%;
    width: 200px;
  }

  .fr-table {
    min-width: 300px;
  }
`;
