// Reserved for future module-level type augmentations local to the map module.
// (See `legend/tools/useDrawingTool.ts` for the wrapper that smooths over the
// `@types/mapbox__mapbox-gl-draw` overload limitation around `changeMode`.)
export {};
