import { fbEventsHandler } from '@rivercode/facebook-conversion-api-nextjs/handlers';

// this route is a proxy to the Facebook Conversion API which also tracks custom browser events.
export default fbEventsHandler;
